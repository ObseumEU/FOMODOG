namespace FomoDog
{
    public static class FeatureFlags
    {
        /// <summary>
        /// This bot separates the chat history by tenant (group, session etc..).
        /// </summary>
        public const string MULTI_TENTANT_BOT = "MultiTenantBot";
    }
}
